{
    "data": [
        {{ range $index, $e := .Data.Pages }}
        {{ if $index }}, {{ end }} {
            "title" : {{ .Title | jsonify }},
            "link" : {{ .Permalink | jsonify }}
        }
        {{ end }}
    ]
}
